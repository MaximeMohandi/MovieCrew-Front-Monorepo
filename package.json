{
  "private": true,
  "scripts": {
    "test": "turbo run test",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "@turbo/gen": "^@turbo/gen",
    "eslint": "^8.0.0",
    "eslint-config-custom": "*",
    "prettier": "^3.0.0",
    "turbo": "^2.1.2"
  },
  "name": "moviecrew-front-monorepo",
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
